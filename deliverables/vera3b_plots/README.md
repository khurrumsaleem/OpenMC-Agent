# VERA 3B 渲染图交付

来源运行：

`data/runs/VERA_3B`

该运行的 `workflow_outcome.json` 为 `status=ok`，`capability_report.json` 为 `renderability=runnable`、`supported_renderer=assembly`、`is_executable=true`。本目录只整理代表性 PNG；原始 OpenMC XML、statepoint、summary、voxel 和完整运行 artifacts 保留在 `data/runs/VERA_3B`。

## 文件说明

- `vera3b_render_contact_sheet.png`：6 个 VERA 3B 渲染切片的总览拼图。
- `assembly_xy_material.png`：组件 XY 切片，按 material 着色。
- `assembly_xy_cell.png`：组件 XY 切片，按 cell 着色。
- `assembly_xz_material.png`：组件 XZ 轴向切片，按 material 着色。
- `assembly_xz_cell.png`：组件 XZ 轴向切片，按 cell 着色。
- `verification_xz_material.png`：verification XZ 轴向切片，按 material 着色。
- `verification_xz_cell.png`：verification XZ 轴向切片，按 cell 着色。
